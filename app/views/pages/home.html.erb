<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>

<ul>
  <% @users.each do |user| %>
    <strong><%= link_to user.first_name, booking_path(user) %></strong>
    <li><%= user.task %></li>
    <li><%= user.hourly_price %></li>
  <% end %>
</ul>
